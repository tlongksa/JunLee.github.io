#include<stdio.h>
#include<math.h>
int main() {
	int n = 0;
	printf("Enter value n : ");
	scanf("%2d", &n);
	int array[n];
	printf("Enter value of array[%d]", i);
	scanf("%2d", &array[i]);
	int x = 0, max = 0;
	printf("Enter value X : ");
	scanf("%2d", &x);
	for (int i = 0; i < n; i++) {
		int array[0] = max;
		if (array[i] > max)
		max = array[i];
	}
	return 0;
}

